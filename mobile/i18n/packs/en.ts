import { en as app } from '../messages/app';
import { en as auth } from '../messages/auth';
import { en as common } from '../messages/common';
import { en as errors } from '../messages/errors';
import { en as feed } from '../messages/feed';
import { en as profile } from '../messages/profile';
import { en as reader } from '../messages/reader';

export default { app, auth, common, errors, feed, profile, reader };
