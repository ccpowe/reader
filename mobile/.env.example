# The mobile app has no static server or Supabase connection variables.
# Enter one Reader base URL in the app before logging in; it discovers the
# runtime API and public Supabase key from /.well-known/reader.json.
#
# This file is intentionally kept as a safe, empty Expo dotenv example so
# legacy build-time connection values cannot silently select a server or
# identity.
